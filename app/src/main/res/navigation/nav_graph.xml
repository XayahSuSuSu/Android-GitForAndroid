<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/HomeFragment">

    <fragment
        android:id="@+id/HomeFragment"
        android:name="com.xayah.gitforandroid.fragment.home.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home">
    </fragment>
    <fragment
        android:id="@+id/RepoFragment"
        android:name="com.xayah.gitforandroid.fragment.repo.RepoFragment"
        android:label="@string/repo">
    </fragment>
    <fragment
        android:id="@+id/AccountsFragment"
        android:name="com.xayah.gitforandroid.fragment.accounts.AccountsFragment"
        android:label="@string/accounts">
    </fragment>
    <fragment
        android:id="@+id/MoreFragment"
        android:name="com.xayah.gitforandroid.fragment.more.MoreFragment"
        android:label="@string/more"
        tools:layout="@layout/fragment_more">
    </fragment>
</navigation>